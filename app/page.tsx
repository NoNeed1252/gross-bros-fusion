'use client'\n\nimport { useState } from \"react\"\nimport { PortalHeader, PortalNav } from \"@/components/portal-nav\"\nimport { TradeTab } from \"@/components/tabs/trade-tab\"\nimport { ChatTab } from \"@/components/tabs/chat-tab\"\nimport { WalletTab } from \"@/components/tabs/wallet-tab\"\nimport { ArcadeTab } from \"@/components/tabs/arcade-tab\"\n\nexport default function Page() {\n  const [tab, setTab] = useState<\'vault\' | \'trade\' | \'chat\' | \'wallet\' | \'arcade\'>('chat')\n\n  return (\n    <div className=\"portal-bg min-h-dvh flex flex-col\">\n      <PortalHeader activeTab={tab} setActiveTab={setTab} />\n      <main className=\"flex-1 p-4\">\n        {tab === 'vault' && <TradeTab />}\n        {tab === 'trade' && <TradeTab />}\n        {tab === 'chat' && <ChatTab />}\n        {tab === 'wallet' && <WalletTab />}\n        {tab === 'arcade' && <ArcadeTab />}\n      </main>\n      <PortalNav activeTab={tab} setActiveTab={setTab} />\n    </div>\n  )\n}\n